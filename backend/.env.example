# App settings
APP_NAME=omnis-backend
HOST=0.0.0.0
PORT=8000

# Database settings - used by the backend service
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=omni
DATABASE_PASSWORD=omni_password
DATABASE_NAME=omnis

# Docker Postgres environment - keep synced with DATABASE_*
POSTGRES_USER=omni
POSTGRES_PASSWORD=omni_password
POSTGRES_DB=omnis
